<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >
    <TextView
        android:id="@+id/rowDate"
        android:background="@color/notepad_paper"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:padding="10dp"
        android:textSize="14dp"
        android:scrollbars="vertical"
        android:fadingEdge="vertical"
        android:textColor="#F000"
        android:layout_alignParentRight="true"
        />

    <com.example.truebelief.anglesurveyor.ExistingJobView
        android:id="@+id/row"
        android:padding="10dp"
        android:textSize="20dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical"
        android:fadingEdge="vertical"
        android:textColor="@color/notepad_text"
        android:layout_toLeftOf="@+id/rowDate"
        />
    <Button
        android:layout_width="@dimen/notepad_margin"
        android:layout_height="wrap_content"
        android:id="@+id/rowExpand"
        android:background="@null"
        android:textSize="20dp"
        android:visibility="invisible"
        />
    <ListView android:layout_height="fill_parent" android:layout_width="fill_parent" android:id="@android:id/list"></ListView>
</RelativeLayout>